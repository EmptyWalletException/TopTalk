spring:
  cloud:
    config:
      name: recruit
      profile: dev
      label: master
      discovery:
        enabled: true
        service-id: toptalk-config
eureka:
  client:
    service-url:
      defaultZone: http://localhost:6868/eureka/